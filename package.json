{
  "name": "voxfit-post-call-decision-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic decision engine for post-call signals (Voxfit take-home).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.3",
  "dependencies": {
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
